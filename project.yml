# XcodeGen spec — generates Avasia-iOS.xcodeproj for xcodebuild / Xcode.
# Install: brew install xcodegen
# Generate: xcodegen generate
name: Avasia-iOS
options:
  bundleIdPrefix: com.jacobrozell
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true
  groupOrdering:
    - order: [App, Config, Sources, Tests]
  developmentLanguage: en

configFiles:
  Debug: Config/Signing.xcconfig
  Release: Config/Signing.xcconfig

packages:
  AvasiaEngine:
    path: .

targets:
  Avasia-iOS:
    type: application
    platform: iOS
    sources:
      - path: App
        excludes:
          - "**/Info.plist"
          - Resources/Assets.xcassets
    resources:
      - path: App/Resources/Assets.xcassets
      - path: App/Resources/Audio
    dependencies:
      - package: AvasiaEngine
        product: AvasiaEngine
      - package: AvasiaEngine
        product: AvasiaSoCEngine
    settings:
      base:
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: App/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_INCLUDE_ALL_APPICON_ASSETS: YES
        INFOPLIST_KEY_CFBundleDisplayName: Avasia
        PRODUCT_BUNDLE_IDENTIFIER: com.jacobrozell.avasia-ios
        PRODUCT_NAME: Avasia
        MARKETING_VERSION: "0.1.0"
        CURRENT_PROJECT_VERSION: "1"
        TARGETED_DEVICE_FAMILY: "1,2"
        SWIFT_VERSION: "5.9"
        ENABLE_USER_SCRIPT_SANDBOXING: NO
    info:
      path: App/Info.plist
      properties:
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UILaunchScreen:
          UIImageName: AvasiaLogo
          UIColorName: LaunchBackground

schemes:
  Avasia-iOS:
    build:
      targets:
        Avasia-iOS: all
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: false
      targets: []
